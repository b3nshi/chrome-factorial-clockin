{
  "name": "Factorial-AutoFill",
  "version": "1.0.0",
  "description": "It is a simple Chrome extension developed to autofill the clock-in option in Factorial.",
  "main": "popup.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel popup.html",
    "build": "rm -rf dist .cache && parcel build popup.html && cp manifest.json dist/manifest.json && cp -rf images dist/images"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "browserslist": [
    "last 3 chrome versions"
  ]
}
